// Il programma dovrà consentire di calcolare il prezzo del panino selezionando
// o deselezionando gli ingredienti proposti.

var ingredients = [
    ["cheese", '2.5']
    ["eggs", '2']
    ['mustard', '0.5']
    ['tomato', '1']
    ['lettuce', '1']
    ['ketchup', '0.5']
]

function renderIngredients(el, price) {
    var counter = 0;
    while (counter < ingredients.length) {

    }
}
var dataPrice = document.getAttribute('data-price')
renderIngredients()